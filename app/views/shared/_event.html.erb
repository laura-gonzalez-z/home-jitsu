<div class="container py-4">
    <div class="">
      <div class="">
        <div>
          <p><strong>Title:</strong><%= event.title %></p>
          <p><strong>Description: </strong><%= event.description %></p>
          <p><strong>Status: <%= event.status %></strong></p>
          <% if event.photo.key %>
            <%= cl_image_tag event.photo.key, crop: :thumb %>
          <% end %>
        </div>
      </div>
    </div>
</div>
