<h1>Your partners</h1>
<ul>
  <% current_user.friends.each do |friend| %>
    <li><%= link_to "#{friend.first_name} #{friend.last_name}", partner_path(friend), class: "text-decoration-none"%></li>
  <% end %>
</ul>
