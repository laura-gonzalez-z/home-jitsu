<%= simple_form_for(@event) do |f| %>
  <%= f.input :date, as: :date, html5: true %>
  <%= f.input :status, collection: ["Open", "Closed"] %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.submit %>
<% end %>
